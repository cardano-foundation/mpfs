{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"Introduction to MPFS","text":""},{"location":"#merkle-patricia-forestry","title":"Merkle Patricia Forestry","text":"<p>A Merkle Patricia Forestry (MPF) is a data structure that allows storing key-value pairs (facts) in a verifiable and immutable way on a blockchain. For a primer on MPF, see CF Presentation.</p> <p>Storing facts inside an MPF provides cryptographic proofs of inclusion and exclusion for each fact, allowing anyone to verify the integrity of the data. When an MPF is available inside a UTxO on the Cardano blockchain, transactions can refer to it as input to pass smart contract validation.</p> <p>How to provide and access MPF's facts and how to publish the MPF root is left to be decided. MPFS takes some decisions to provide a complete solution to manage MPF tokens on Cardano.</p>"},{"location":"#design-choices-to-run-mpf-on-cardano","title":"Design choices to run MPF on Cardano","text":"<ul> <li>All modifications to an MPF root have to appear on-chain. This gives anyone with access to the blockchain history the ability to reconstruct the full history of changes and so the current facts.</li> <li>All modifications must be consumed under a smart contract validation. This eliminates the burden to prove that an MPF contains only facts that are the result of modifications that have been on chain.</li> <li>MPF are owned. This gives the smart contract the ability to operate on multiple MPF tokens, each controlled by a different user. Products of MPF roots and owners are referred to as MPF tokens from now on.</li> <li>MPF token can be modified only by their owner. This gives full control to the owner on which facts are part of the MPF token. The owner of an MPF token is referred to as <code>oracle</code> from now on.</li> <li>MPF tokens have a unique identifier. This gives the ability to address a specific MPF token inside a modification.</li> <li>Specific MPF modifications are owned. This gives the modification requester the ability to retract modifications if they are not accepted by the owner. The owner of a modification request is referred to as <code>requester</code> from now on.</li> </ul>"},{"location":"#mpfs-the-service","title":"MPFS: the service","text":"<p>MPFS is an http service wrapping:</p> <ul> <li>MPF operations from the Merkle Patricia Forestry library in one smart contract to track and validate each MPF history of changes</li> <li>An off-chain transaction builder that helps building transactions to interact with the smart contract</li> <li>An indexer over the smart contract events to reconstruct and serve the MPF state</li> </ul> <p>It is designed to be used by anyone who wants to store and manage knowledge in a decentralized manner, allowing contributors to add or remove facts while ensuring the integrity and history of the knowledge database.</p> <p>It is particularly useful for applications that require a verifiable and immutable record of knowledge, such as decentralized applications (dApps), knowledge management systems, and collaborative platforms.</p> <p>The service comes in 2 flavors: signingful and signingless. The signingful version is suitable for local installations where it controls a private key for the user, while the signingless version is suitable for remote installations where the user has to provide a signature for each transaction.</p>"},{"location":"#quick-links","title":"Quick Links","text":"<ul> <li>Getting Started - Installation and setup</li> <li>Architecture - System design and components</li> <li>Signingless Manual - Production API usage guide</li> <li>API Reference - OpenAPI documentation</li> </ul>"},{"location":"architecture/","title":"System Architecture","text":"<pre><code>architecture-beta\n\n    service chain(internet)[Cardano Blockchain]\n    group alice(cloud)[alice Box]\n        service aliceMPFS(server)[alice MPFS Service] in alice\n        service requester1(server)[alice client] in alice\n        requester1:R -- L:aliceMPFS\n    aliceMPFS:R -- L:chain\n\n    service bobMPFS(server)[public MPFS Service]\n    service requester2(server)[bob client]\n    requester2:R -- L:bobMPFS\n\n    service requester3(server)[willy client]\n    requester3:L -- R:bobMPFS\n\n    bobMPFS:T -- B:chain\n    group oracle(cloud)[oracle Box]\n        service oracleMPFS(server)[oracle MPFS Service] in oracle\n        service oracleClient(server)[oracle client] in oracle\n        oracleClient:L -- R:oracleMPFS\n    oracleMPFS:B -- T:chain</code></pre>"},{"location":"architecture/#components","title":"Components","text":""},{"location":"architecture/#mpf-token","title":"MPF Token","text":"<p>The MPF token is a native token living on the Cardano blockchain. It is associated with a merkle trie root and the public key hash of the oracle controlling it.</p>"},{"location":"architecture/#mpf-token-facts","title":"MPF Token Facts","text":"<p>Facts are key-value pairs stored in the merkle trie associated with an MPF token. They represent the knowledge stored in the database.</p>"},{"location":"architecture/#parties","title":"Parties","text":""},{"location":"architecture/#blockchain","title":"Blockchain","text":"<p>The blockchain is used to store the state of the knowledge database and to validate operations on it.</p>"},{"location":"architecture/#oracle","title":"Oracle","text":"<p>Someone who controls which facts are part of an MPF token.</p>"},{"location":"architecture/#requester","title":"Requester","text":"<p>Anyone who wants to change the facts in an MPF token.</p>"},{"location":"architecture/#observer","title":"Observer","text":"<p>Anyone who needs to query the knowledge database and/or use any fact as input to a smart contract.</p>"},{"location":"architecture/#overview","title":"Overview","text":""},{"location":"architecture/#facts-storage","title":"Facts Storage","text":"<p>All knowledge is stored on the blockchain history, so it's reconstructable by anyone with access to it. Moreover each of its facts are referable as transaction inputs to be consumed in smart contracts as if they were stored in the current state of the blockchain.</p>"},{"location":"architecture/#token-ownership","title":"Token Ownership","text":"<p>The token owner (a.k.a. oracle) is the only one who can add or remove facts from the database. To do so he has to consume requests from requesters. In the current shape of the system, the oracle has full control over the facts, but in the future it could be possible to implement a more decentralized model where this power is distributed among multiple parties.</p>"},{"location":"architecture/#interaction","title":"Interaction","text":""},{"location":"architecture/#mpf-token_1","title":"MPF Token","text":"<p>We refer to the current state of the knowledge as the \"MPF token\". The token is the only value that is part of the blockchain state and it contains the hash of the current facts and the public key of the oracle controlling it. All tokens are sitting at the system address which is determined by the smart contract controlling its identifier and its updating.</p>"},{"location":"architecture/#token-transactions","title":"Token Transactions","text":"<p>3 transactions are possible involving tokens:</p> <ol> <li>Boot: Anyone can become an oracle by creating a new MPF token. This token will be started with the null hash and be uniquely identified by an id, the token-id. The oracle will also set the public key of the oracle in the token.</li> <li>Update: The oracle can update the MPF token by adding or removing facts from the database. This transaction will consume requests from contributors and will update the knowledge hash in the token and potentially change the ownership of the token.</li> <li>End: The oracle can delete the MPF token, which will remove the token-id, so no other updates can be applied on it. Future versions of this system will allow to use a deleted token knowledge hash as a starting point for a new MPF token, so the knowledge can be transferred in a new token without losing the history of the previous one.</li> </ol>"},{"location":"architecture/#change-requests","title":"Change Requests","text":"<p>Facts contributors can send requests to the system address. They will be spent in one update token transaction of the token they are referring to. The request will contain the key and the value to be added or removed from the knowledge database as well as the token-id of the MPF token they want to be applied to.</p> <p>The request will also contain the public key of the requester, so:</p> <ul> <li>The request publisher can retract the request if needed.</li> <li>The oracle knows how to restitute part of the value that is locked in the request to the contributor.</li> </ul> <p>3 Transactions are possible involving requests:</p> <ol> <li>Request: Anyone can create a request to add or remove knowledge from the database. The request will be stored in the blockchain state and can be consumed by the oracle in the update transaction.</li> <li>Retract: The requester can retract the request in case the oracle does not accept it.</li> <li>Update: This is the same transaction as the update of the MPF token, which consumes both a token and the requests that were targeting it.</li> </ol>"},{"location":"architecture/#smart-contracts","title":"Smart Contracts","text":"<p>A smart contract is involved in <code>boot</code>, <code>update</code>, <code>end</code>, and <code>retract</code> transactions. For each of these transactions, the smart contract will enforce rules to ensure the integrity of the knowledge database and the validity of the transactions.</p> <ul> <li>boot:</li> <li>The new token-id is unique</li> <li>The hash in the token is null (this is the hash of the knowledge database, implying an empty database)</li> <li>update:</li> <li>All consumed change-requests have their referred token consumed as well</li> <li>All requests are coupled with a proof that the update is actually performed to the knowledge database</li> <li>The token remains at the system address</li> <li>The token owner is the one who is performing the update</li> <li>end:</li> <li>The token owner is the one who is performing the end</li> <li>retract:</li> <li>The request publisher is the one who is performing the retract</li> </ul>"},{"location":"architecture/#protocol-example","title":"Protocol Example","text":"<pre><code>sequenceDiagram\n    participant A as Alice\n    participant C as Bob\n    participant B as Blockchain\n    participant O as Oracle\n    participant Pub as Anyone\n\n    O-&gt;&gt;B: Boot MPF Token X\n\n    A-&gt;&gt;B: Request to Insert Fact A\n    C-&gt;&gt;B: Request to Insert Fact B\n\n    O-&gt;&gt;B: Update MPF Token X\n\n    Pub-&gt;&gt;B: Observe MPF Token X\n    B--&gt;&gt;Pub: Return Facts A, B\n\n    A-&gt;&gt;B: Delete Fact B Request\n    O-&gt;&gt;B: Update MPF Token\n    Pub-&gt;&gt;B: Observe MPF Token X\n    B--&gt;&gt;Pub: Return Fact A\n\n    C-&gt;&gt;B: Delete Fact A Request\n    C-&gt;&gt;B: Retract Change Request to Delete Fact A\n    O-&gt;&gt;B: Update MPF Token X\n    Pub-&gt;&gt;B: Observe MPF Token X\n    B--&gt;&gt;Pub: Return Fact A</code></pre>"},{"location":"getting-started/","title":"Getting Started","text":""},{"location":"getting-started/#pre-requisites","title":"Pre-requisites","text":"<p>Being an HTTP service no particular skill is required to use it, but some knowledge of Cardano transactions and smart contracts is useful to understand how to interact with it.</p> <p>Deployment has some external requirements:</p> <ol> <li>Yaci store. This is required to provide address to UTxO mapping. When building transactions the user will provide an address that he controls and wants to spend from. The service will use the Yaci store to find the UTxOs that are associated with that address and build the transaction accordingly.</li> <li>Ogmios. This is required to track the events involving the system address.</li> <li>Cardano node. This is indirectly required by both Yaci store and Ogmios.</li> </ol> <p>The MPFS service in itself is a TypeScript application so any Node.js supported platform should be able to run it. It is tested on Linux and macOS, but it should work on Windows as well.</p>"},{"location":"getting-started/#docker","title":"Docker","text":"<p>The image is available on ghcr.io and can be pulled with the command:</p> <pre><code>docker pull ghcr.io/cardano-foundation/mpfs/mpfs:v1.1.0\n</code></pre> <p>This is an example of a working deployment: mpfs in CF</p> <p>Consider sourcing bootstrap.sh as mentioned in setup instructions to speedup the node syncing.</p>"},{"location":"getting-started/#from-source","title":"From Source","text":"<p>Alternatively, you can run the service from source. To do so, you need to have Node.js and npm installed on your system. Then you can clone the repository and run the following commands:</p> <pre><code>git clone https://github.com/cardano-foundation/mpfs\ncd off_chain\nnpm install\nnpx tsx src/service/signingless/main.ts --port 3000 \\\n    --provider yaci --yaci-store-host http://localhost:8080 \\\n    --ogmios-host http://localhost:1337 \\\n    --database-path ./mpfs.db \\\n    --since-slot 94898393 \\\n    --since-block-id ef94934f8eb129ebf07eeaab007b81ecb1bc58b121d19ac0ffe81f928bf56cc\n</code></pre> <p>This will start the service on port 3000, using the Yaci store running on <code>http://localhost:8080</code> and the Ogmios server running on <code>http://localhost:1337</code>. The database will be stored in the file <code>mpfs.db</code> in the current directory. The <code>since-slot</code> and <code>since-block-id</code> parameters are used to specify the starting point for the service to track events.</p> <p>Warning</p> <p>Be careful to start indexing before the token you are going to use was created. If you are creating a new token, just start from \"now-ish\".</p>"},{"location":"getting-started/#usage","title":"Usage","text":"<ul> <li>Signingless API Reference</li> <li>Signingless Manual</li> <li>Signingful Manual</li> </ul>"},{"location":"swagger-ui/","title":"API Reference","text":""},{"location":"swagger-ui/#live-api-documentation","title":"Live API Documentation","text":"<p>The interactive API documentation is available at:</p> <p>https://mpfs.plutimus.com/api-docs</p> <p>A public preprod instance is running at https://mpfs.plutimus.com.</p>"},{"location":"swagger-ui/#api-overview","title":"API Overview","text":""},{"location":"swagger-ui/#configuration","title":"Configuration","text":"Endpoint Method Description <code>/config</code> GET Get service configuration including network and policy ID"},{"location":"swagger-ui/#tokens","title":"Tokens","text":"Endpoint Method Description <code>/tokens</code> GET List all tracked MPF tokens <code>/token/{tokenId}</code> GET Get details of a specific token <code>/token/{tokenId}/facts</code> GET Get all facts stored in a token's MPF"},{"location":"swagger-ui/#transactions","title":"Transactions","text":"<p>All transaction endpoints return unsigned CBOR transactions that must be signed by the user's wallet.</p> Endpoint Method Description <code>/transaction/{address}/boot-token</code> GET Build a transaction to create a new MPF token <code>/transaction/{address}/request-insert/{tokenId}</code> POST Build a transaction to request inserting a fact <code>/transaction/{address}/request-update/{tokenId}</code> POST Build a transaction to request updating a fact <code>/transaction/{address}/request-delete/{tokenId}</code> POST Build a transaction to request deleting a fact <code>/transaction/{address}/update-token/{tokenId}</code> GET Build a transaction to process pending requests <code>/transaction/{address}/retract-change/{outputRef}</code> GET Build a transaction to retract a pending request <code>/transaction/{address}/end-token/{tokenId}</code> GET Build a transaction to destroy a token <code>/transaction</code> POST Submit a signed transaction <code>/transaction</code> GET Get transaction info by hash"},{"location":"swagger-ui/#status","title":"Status","text":"Endpoint Method Description <code>/tips</code> GET Get current network and indexer tips <code>/wait/{nBlocks}</code> GET Wait for specified number of blocks"},{"location":"swagger-ui/#authentication","title":"Authentication","text":"<p>The API is public and does not require authentication. However, all state-changing operations require transaction signatures from the user's wallet.</p>"},{"location":"swagger-ui/#error-handling","title":"Error Handling","text":"<p>The API returns standard HTTP status codes:</p> <ul> <li><code>200</code>: Success</li> <li><code>400</code>: Bad request (invalid parameters)</li> <li><code>404</code>: Resource not found</li> <li><code>500</code>: Internal server error</li> </ul> <p>Error responses include a JSON body with <code>error</code> and <code>details</code> fields.</p>"},{"location":"swagger-ui/#openapi-specification","title":"OpenAPI Specification","text":"<p>The full OpenAPI specification is available at:</p> <ul> <li>Live: https://mpfs.plutimus.com/api-docs</li> <li>JSON: openapi.json</li> </ul>"},{"location":"code/off-chain/","title":"Off-chain Code Documentation","text":"<p>The off-chain component is written in TypeScript and provides transaction building, chain indexing, and HTTP API services.</p>"},{"location":"code/off-chain/#overview","title":"Overview","text":"<p>The off-chain code consists of several modules:</p> <ul> <li>Transaction Builders: Functions to construct Cardano transactions</li> <li>Indexer: Tracks cage contract events on-chain</li> <li>Trie Manager: Manages local MPF state for proof generation</li> <li>HTTP Service: REST API for interacting with MPFS</li> </ul>"},{"location":"code/off-chain/#module-structure","title":"Module Structure","text":""},{"location":"code/off-chain/#transaction-context-transactionscontextts","title":"Transaction Context (<code>transactions/context.ts</code>)","text":"<p>The <code>Context</code> type bundles all dependencies needed for transaction building:</p> <pre><code>type Context = {\n  cagingScript: { cbor, address, scriptHash, policyId };\n  signingWallet: SigningWallet | undefined;\n  addressWallet: (address: string) =&gt; Promise&lt;WalletInfo&gt;;\n  newTxBuilder: () =&gt; MeshTxBuilder;\n  fetchTokens: () =&gt; Promise&lt;Token[]&gt;;\n  fetchToken: (tokenId: string) =&gt; Promise&lt;CurrentToken | undefined&gt;;\n  fetchRequests: (tokenId: string | null) =&gt; Promise&lt;Request[]&gt;;\n  trie: (tokenId: string, f: (trie: SafeTrie) =&gt; Promise&lt;any&gt;) =&gt; Promise&lt;void&gt;;\n  waitBlocks: (n: number) =&gt; Promise&lt;number&gt;;\n  tips: () =&gt; Promise&lt;{ networkTip, indexerTip }&gt;;\n  waitSettlement: (txHash: string) =&gt; Promise&lt;string&gt;;\n  pauseIndexer: () =&gt; Promise&lt;() =&gt; void&gt;;\n  submitTx: (txHex: string) =&gt; Promise&lt;string&gt;;\n  // ...\n};\n</code></pre>"},{"location":"code/off-chain/#transaction-builders","title":"Transaction Builders","text":""},{"location":"code/off-chain/#boot-transactionsbootts","title":"Boot (<code>transactions/boot.ts</code>)","text":"<p>Creates a new MPF token:</p> <pre><code>async function boot(context: Context): Promise&lt;WithTxHash&lt;string&gt;&gt;\n</code></pre> <ul> <li>Mints a new token with unique ID (from spent UTxO)</li> <li>Creates State UTxO at cage address</li> <li>Initializes with null MPF root</li> </ul>"},{"location":"code/off-chain/#request-transactionsrequestts","title":"Request (<code>transactions/request.ts</code>)","text":"<p>Submits a modification request:</p> <pre><code>async function request(\n  context: Context,\n  tokenId: string,\n  change: UnslottedChange\n): Promise&lt;WithTxHash&lt;null&gt;&gt;\n</code></pre> <ul> <li>Creates Request UTxO at cage address</li> <li>Locks 10 ADA (refundable)</li> <li>Contains operation details (insert/delete/update)</li> </ul>"},{"location":"code/off-chain/#update-transactionsupdatets","title":"Update (<code>transactions/update.ts</code>)","text":"<p>Processes pending requests:</p> <pre><code>async function update(\n  context: Context,\n  tokenId: string,\n  requireds: OutputRef[] = []\n): Promise&lt;WithTxHash&lt;string | null&gt;&gt;\n</code></pre> <ul> <li>Spends State UTxO with Modify redeemer</li> <li>Spends selected Request UTxOs with Contribute redeemer</li> <li>Generates proofs by temporarily applying changes to local trie</li> <li>Produces new State UTxO with updated root</li> </ul>"},{"location":"code/off-chain/#retract-transactionsretractts","title":"Retract (<code>transactions/retract.ts</code>)","text":"<p>Reclaims a pending request:</p> <pre><code>async function retract(\n  context: Context,\n  requestOutputRef: OutputRef\n): Promise&lt;WithTxHash&lt;null&gt;&gt;\n</code></pre> <ul> <li>Spends Request UTxO with Retract redeemer</li> <li>Returns locked ADA to request owner</li> </ul>"},{"location":"code/off-chain/#end-transactionsendts","title":"End (<code>transactions/end.ts</code>)","text":"<p>Destroys an MPF token:</p> <pre><code>async function end(\n  context: Context,\n  tokenId: string\n): Promise&lt;WithTxHash&lt;null&gt;&gt;\n</code></pre> <ul> <li>Spends State UTxO with End redeemer</li> <li>Burns the token (mints -1)</li> <li>Requires owner signature</li> </ul>"},{"location":"code/off-chain/#trie-management","title":"Trie Management","text":""},{"location":"code/off-chain/#change-types-triechangets","title":"Change Types (<code>trie/change.ts</code>)","text":"<pre><code>type UnslottedChange =\n  | { type: 'insert'; key: string; newValue: string }\n  | { type: 'delete'; key: string; oldValue: string }\n  | { type: 'update'; key: string; oldValue: string; newValue: string };\n</code></pre>"},{"location":"code/off-chain/#safetrie-triesafetriets","title":"SafeTrie (<code>trie/safeTrie.ts</code>)","text":"<p>Provides safe trie operations with rollback capability:</p> <pre><code>type SafeTrie = {\n  root: () =&gt; Uint8Array | null;\n  get: (key: string) =&gt; Promise&lt;string | null&gt;;\n  insert: (key: string, value: string) =&gt; Promise&lt;Proof&gt;;\n  delete: (key: string, value: string) =&gt; Promise&lt;Proof&gt;;\n  update: (key: string, oldValue: string, newValue: string) =&gt; Promise&lt;Proof&gt;;\n  temporaryUpdate: (change: UnslottedChange) =&gt; Promise&lt;Proof&gt;;\n  allFacts: () =&gt; Promise&lt;Record&lt;string, ValueSlotted&gt;&gt;;\n  rollback: () =&gt; Promise&lt;void&gt;;\n};\n</code></pre>"},{"location":"code/off-chain/#indexer","title":"Indexer","text":"<p>The indexer tracks on-chain events via Ogmios and maintains local state:</p> <ul> <li>Tokens: Tracks all MPF tokens and their current state</li> <li>Requests: Tracks pending modification requests</li> <li>Events: Processes block roll-forward/roll-back events</li> </ul>"},{"location":"code/off-chain/#http-service","title":"HTTP Service","text":""},{"location":"code/off-chain/#signingless-api","title":"Signingless API","text":"<p>The production API that returns unsigned transactions:</p> Endpoint Method Description <code>/config</code> GET Service configuration <code>/tokens</code> GET List all tokens <code>/token/{tokenId}</code> GET Get token details <code>/token/{tokenId}/facts</code> GET Get token facts <code>/transaction/{address}/boot-token</code> GET Build boot transaction <code>/transaction/{address}/request-insert/{tokenId}</code> POST Build insert request <code>/transaction/{address}/request-update/{tokenId}</code> POST Build update request <code>/transaction/{address}/request-delete/{tokenId}</code> POST Build delete request <code>/transaction/{address}/update-token/{tokenId}</code> GET Build update transaction <code>/transaction/{address}/retract-change/{outputRef}</code> GET Build retract transaction <code>/transaction/{address}/end-token/{tokenId}</code> GET Build end transaction <code>/transaction</code> POST Submit signed transaction"},{"location":"code/off-chain/#architecture","title":"Architecture","text":"<pre><code>flowchart TB\n    subgraph Client\n        Wallet[User Wallet]\n    end\n\n    subgraph MPFS Service\n        API[HTTP API]\n        TxBuilder[Transaction Builder]\n        Indexer[Chain Indexer]\n        TrieManager[Trie Manager]\n        State[(SQLite State)]\n    end\n\n    subgraph External\n        Yaci[Yaci Store]\n        Ogmios[Ogmios]\n        Node[Cardano Node]\n    end\n\n    Wallet --&gt; API\n    API --&gt; TxBuilder\n    TxBuilder --&gt; TrieManager\n    Indexer --&gt; State\n    TrieManager --&gt; State\n    Indexer --&gt; Ogmios\n    TxBuilder --&gt; Yaci\n    Ogmios --&gt; Node\n    Yaci --&gt; Node</code></pre>"},{"location":"code/off-chain/#key-patterns","title":"Key Patterns","text":""},{"location":"code/off-chain/#proof-generation","title":"Proof Generation","text":"<p>When processing requests, the service:</p> <ol> <li>Pauses the indexer to prevent state changes</li> <li>Temporarily applies each change to the local trie copy</li> <li>Captures the proof for each operation</li> <li>Includes proofs in the Modify redeemer</li> <li>Rolls back the trie after transaction is built</li> </ol>"},{"location":"code/off-chain/#atomic-operations","title":"Atomic Operations","text":"<p>The <code>pauseIndexer()</code> function ensures atomic operations:</p> <pre><code>const release = await context.pauseIndexer();\ntry {\n  // Perform operations\n} finally {\n  release();\n}\n</code></pre>"},{"location":"code/off-chain/#transaction-submission","title":"Transaction Submission","text":"<p>Uses Ogmios for reliable transaction submission with automatic retries.</p>"},{"location":"code/on-chain/","title":"On-chain Code Documentation","text":"<p>The on-chain component is written in Aiken, a functional programming language designed for Cardano smart contracts.</p>"},{"location":"code/on-chain/#overview","title":"Overview","text":"<p>The cage smart contract manages MPF (Merkle Patricia Forestry) tokens on Cardano. It implements a \"caging\" pattern where tokens are locked at a script address with their MPF root stored in the datum.</p>"},{"location":"code/on-chain/#module-structure","title":"Module Structure","text":""},{"location":"code/on-chain/#typesak","title":"<code>types.ak</code>","text":"<p>Defines the core data types used throughout the contract:</p>"},{"location":"code/on-chain/#mint","title":"<code>Mint</code>","text":"<p>Represents the type of minting operation: - <code>Boot</code> - Creates a new MPF token with an empty root - <code>Destroy</code> - Burns an existing MPF token</p>"},{"location":"code/on-chain/#mintredeemer","title":"<code>MintRedeemer</code>","text":"<p>Redeemer for minting policy validation: <pre><code>type MintRedeemer {\n  Boot { token: TokenId }\n  Destroy\n}\n</code></pre></p>"},{"location":"code/on-chain/#updateredeemer","title":"<code>UpdateRedeemer</code>","text":"<p>Redeemer for spending validator (state updates): <pre><code>type UpdateRedeemer {\n  proofs: List&lt;Proof&gt;\n}\n</code></pre></p>"},{"location":"code/on-chain/#state","title":"<code>State</code>","text":"<p>Represents the current state of an MPF token: <pre><code>type State {\n  owner: ByteArray,  // Public key hash of the owner\n  root: Hash&lt;Blake2b_256, ByteArray&gt;  // Current MPF root hash\n}\n</code></pre></p>"},{"location":"code/on-chain/#operation","title":"<code>Operation</code>","text":"<p>Defines the operations that can be applied to an MPF: <pre><code>type Operation {\n  Insert { new_value: ByteArray }\n  Delete { old_value: ByteArray }\n  Update { old_value: ByteArray, new_value: ByteArray }\n}\n</code></pre></p>"},{"location":"code/on-chain/#request","title":"<code>Request</code>","text":"<p>A pending modification request: <pre><code>type Request {\n  tokenId: TokenId,     // Target token identifier\n  owner: ByteArray,     // Request owner's pub key hash\n  key: ByteArray,       // MPF key to modify\n  operation: Operation  // The requested operation\n}\n</code></pre></p>"},{"location":"code/on-chain/#cagedatum","title":"<code>CageDatum</code>","text":"<p>The datum type for UTxOs at the cage address: <pre><code>type CageDatum {\n  RequestDatum { request: Request }  // A pending request\n  StateDatum { state: State }        // Token state\n}\n</code></pre></p>"},{"location":"code/on-chain/#libak","title":"<code>lib.ak</code>","text":"<p>Helper functions for token manipulation:</p>"},{"location":"code/on-chain/#quantityvalue-policy-name","title":"<code>quantity(value, policy, name)</code>","text":"<p>Extracts the quantity of a specific asset from a Value.</p>"},{"location":"code/on-chain/#assetnamevalue-policy","title":"<code>assetName(value, policy)</code>","text":"<p>Retrieves the asset name of the first token found for a given policy.</p>"},{"location":"code/on-chain/#valuefromtokenpolicy-name-quantity","title":"<code>valueFromToken(policy, name, quantity)</code>","text":"<p>Creates a Value containing the specified token.</p>"},{"location":"code/on-chain/#tokenfromvaluevalue-policy","title":"<code>tokenFromValue(value, policy)</code>","text":"<p>Extracts token information (name and quantity) from a Value.</p>"},{"location":"code/on-chain/#extracttokenfrominputsinputs-policy","title":"<code>extractTokenFromInputs(inputs, policy)</code>","text":"<p>Finds and extracts a token from a list of transaction inputs.</p>"},{"location":"code/on-chain/#cageak","title":"<code>cage.ak</code>","text":"<p>The main validator implementing both minting policy and spending validator.</p>"},{"location":"code/on-chain/#minting-policy","title":"Minting Policy","text":"<p>The minting policy controls token creation and destruction:</p>"},{"location":"code/on-chain/#boot-minting","title":"Boot (Minting)","text":"<p>When creating a new token: 1. The token ID must be unique (derived from a spent UTxO) 2. Exactly one token is minted 3. A new State UTxO is created at the cage address with:    - The minted token    - A null (empty) MPF root    - The signer as the owner</p>"},{"location":"code/on-chain/#destroy-burning","title":"Destroy (Burning)","text":"<p>When destroying a token: 1. The token is burned (quantity: -1) 2. The owner must sign the transaction</p>"},{"location":"code/on-chain/#spending-validator","title":"Spending Validator","text":"<p>The spending validator handles four cases based on the redeemer:</p>"},{"location":"code/on-chain/#end-constr0","title":"End (ConStr0)","text":"<p>Terminates the token: - Validates ownership - Token is burned via the minting policy</p>"},{"location":"code/on-chain/#contribute-constr1","title":"Contribute (ConStr1)","text":"<p>Spends a request UTxO in an update transaction: - Verifies the referenced state UTxO is being consumed - Part of batch request processing</p>"},{"location":"code/on-chain/#modify-constr2","title":"Modify (ConStr2)","text":"<p>Updates the MPF state: - Validates ownership - Verifies all proofs match the requests being consumed - Computes the new root from applied operations - Ensures the output contains the updated state</p>"},{"location":"code/on-chain/#retract-constr3","title":"Retract (ConStr3)","text":"<p>Allows request owners to reclaim their request UTxOs: - Only the request owner can retract - Returns the locked ADA</p>"},{"location":"code/on-chain/#validation-flow","title":"Validation Flow","text":"<pre><code>sequenceDiagram\n    participant User\n    participant Validator\n    participant MPF\n\n    User-&gt;&gt;Validator: Boot Transaction\n    Validator-&gt;&gt;Validator: Verify unique token ID\n    Validator-&gt;&gt;MPF: Create empty state\n\n    User-&gt;&gt;Validator: Request Transaction\n    Validator-&gt;&gt;MPF: Store request at cage address\n\n    User-&gt;&gt;Validator: Update Transaction\n    Validator-&gt;&gt;Validator: Verify ownership\n    Validator-&gt;&gt;Validator: Validate proofs\n    Validator-&gt;&gt;MPF: Apply changes, update root\n\n    User-&gt;&gt;Validator: Retract Transaction\n    Validator-&gt;&gt;Validator: Verify request ownership\n    Validator-&gt;&gt;User: Return locked ADA</code></pre>"},{"location":"code/on-chain/#security-properties","title":"Security Properties","text":"<ol> <li>Ownership: Only the token owner can modify or destroy the token</li> <li>Integrity: All MPF modifications include cryptographic proofs</li> <li>Uniqueness: Token IDs are derived from spent UTxOs, ensuring uniqueness</li> <li>Retractability: Request owners can always reclaim their pending requests</li> </ol>"},{"location":"manual/signingful/","title":"Signingful MPFS Manual","text":"<p>Outdated</p> <p>This documentation is outdated. The signingful version is primarily for local development and testing. For production use, see the Signingless Manual.</p>"},{"location":"manual/signingful/#running-the-service","title":"Running the Service","text":""},{"location":"manual/signingful/#mnemonics","title":"Mnemonics","text":"<p>The service is designed to control a wallet, so you need to expose the mnemonics of the wallet in order to make transactions.</p> <p>The file with the mnemonics is passed as <code>--seed</code> argument. Using the <code>-g</code> option one can overwrite the mnemonics file with the new mnemonics.</p>"},{"location":"manual/signingful/#illustrative-example","title":"Illustrative Example","text":"<p>First, navigate to the off_chain directory and install dependencies:</p> <pre><code>cd off_chain\nnpm install\n</code></pre> <p>To illustrate the example usage of the service, we will use a local cluster with <code>yaci</code>, so that we can easily create wallets and fund them.</p> <p>We will create 3 wallets: <code>charlie</code>, <code>alice</code> and <code>bob</code>. <code>charlie</code> will be the owner of the token, and <code>alice</code> and <code>bob</code> will both request for the updates to the token.</p> <p>First of all, start with <code>nix develop</code> in five separate terminals.</p> <p>Make sure you have yaci up in one terminal (<code>just run-yaci</code>). Then in the other three, representing <code>charlie</code>, <code>alice</code> and <code>bob</code>, respectively, we type the following:</p> <pre><code>npx tsx src/service/main.ts --seed ./mnemonics.txt --provider yaci --port 3000 --yaci-store-host http://localhost:8080 --yaci-admin-host http://localhost:10000 -g\n</code></pre> <pre><code>npx tsx src/service/main.ts --seed ./mnemonics2.txt --provider yaci --port 3002 --yaci-store-host http://localhost:8080 --yaci-admin-host http://localhost:10000 -g\n</code></pre> <pre><code>npx tsx src/service/main.ts --seed ./mnemonics3.txt --provider yaci --port 3004 --yaci-store-host http://localhost:8080 --yaci-admin-host http://localhost:10000 -g\n</code></pre> <p>The <code>-g</code> option enforces using the new mnemonics every time.</p> <p>Now, in the last fifth terminal paste the following:</p> <pre><code>export charlie='http://localhost:3000'\nexport alice='http://localhost:3002'\nexport bob='http://localhost:3004'\n</code></pre> <p>Let's fund <code>charlie</code>, <code>bob</code> and <code>alice</code> wallets:</p> <pre><code>curl -s -X PUT $charlie/wallet/topup -H \"Content-Type: application/json\" -d '{\"amount\": 10000}' | jq\ncurl -s -X PUT $alice/wallet/topup -H \"Content-Type: application/json\" -d '{\"amount\": 10000}' | jq\ncurl -s -X PUT $bob/wallet/topup -H \"Content-Type: application/json\" -d '{\"amount\": 10000}' | jq\n</code></pre> <p>After each command you should see:</p> <pre><code>{\n  \"message\": \"Top up successful\"\n}\n</code></pre> <p>Querying <code>charlie</code> wallet shows that it has some UTxOs:</p> <pre><code>curl -s -X GET $charlie/wallet | jq\n</code></pre>"},{"location":"manual/signingful/#new-tokens","title":"New Tokens","text":"<p>In order to create a new MPF token that <code>charlie</code> controls, he can get his owner id from the retrieved wallet, and use it in token minting.</p> <pre><code>owner=$(curl -s -X GET $charlie/wallet | jq -r '.owner')\necho $owner\n</code></pre> <p>Now <code>charlie</code> can create a token under his control via:</p> <pre><code>tokenId=$(curl -s -X POST $charlie/token \\\n  -H \"Content-Type: application/json\" \\\n  -d \"{\\\"owner\\\": \\\"$owner\\\"}\" | jq -r '.tokenId')\necho $tokenId\n</code></pre> <p>That token id is unique inside the network and can be used to identify the token later. Anyone can always query all tokens at caging address with:</p> <pre><code>curl -s -X GET $alice/tokens | jq\n</code></pre> <p>Take notice that the root is empty, meaning no facts are inside.</p>"},{"location":"manual/signingful/#requests-to-update-a-token","title":"Requests to Update a Token","text":"<p>The request comprises of 4 fields:</p> <ul> <li><code>operation</code>: The operation to perform on the MPF token. This can be one of the following:</li> <li><code>insert</code>: Insert a new value into the MPF.</li> <li><code>delete</code>: Delete a value from the MPF.</li> <li><code>key</code>: The key of the value to update.</li> <li><code>value</code>: The value to be updated at a specified <code>key</code>.</li> <li><code>owner</code>: The owner of the request. This is implicit in the current API.</li> </ul> <p>The owner of the request is (for now) allowed to retract his request whenever he wants.</p> <p>Anyone can create a request to update a token. For example, let <code>bob</code> create a request to update the token we created before.</p> <pre><code>reqBob=$(curl -s -X POST $bob/token/$tokenId/request \\\n  -H \"Content-Type: application/json\" \\\n  -d '{\"operation\": \"insert\",\"key\": \"abc\",\"value\": \"value1\"}' | jq -r)\necho $reqBob\n</code></pre> <p>The request is now in the caging address, and anyone can see it by inspecting the token field <code>requests</code>:</p> <pre><code>curl -s -X GET $alice/token/$tokenId | jq '.requests'\n</code></pre> <p>Let's add a request from <code>alice</code> to insert another value:</p> <pre><code>reqAlice=$(curl -s -X POST $alice/token/$tokenId/request \\\n  -H \"Content-Type: application/json\" \\\n  -d '{\"operation\": \"insert\",\"key\": \"abd\",\"value\": \"value2\"}' | jq -r)\necho $reqAlice\n</code></pre> <p>Let <code>bob</code> retract his request with:</p> <pre><code>curl -s -X DELETE $bob/request/$bobReq | jq\n</code></pre> <p>Now <code>charlie</code> who is the owner of the token can apply the request(s) to the token:</p> <pre><code>curl -s -X PUT $charlie/token/$tokenId  \\\n    -H \"Content-Type: application/json\" \\\n    -d \"{\\\"requestIds\\\":[\\\"$reqAlice\\\"]}\" | jq\n</code></pre> <p>The request is now applied to the token. Anyone can also retrieve the facts:</p> <pre><code>curl -s -X GET $bob/token/$tokenId/facts\n</code></pre>"},{"location":"manual/signingful/#deleting-facts","title":"Deleting Facts","text":"<p>When a fact does not hold anymore, it can be deleted from the MPF token. Again it goes through the request process.</p> <pre><code>delReq=$(curl -s -X POST $bob/token/$tokenId/request \\\n  -H \"Content-Type: application/json\" \\\n  -d '{\"operation\": \"delete\", \"key\": \"abd\", \"value\" : \"value2\"}' | jq -r)\n</code></pre> <p>Then <code>charlie</code> can apply the request to the token:</p> <pre><code>curl -s -X PUT $charlie/token/$tokenId \\\n  -H \"Content-Type: application/json\" \\\n  -d \"{\\\"requestIds\\\":[\\\"$delReq\\\"]}\" | jq\n</code></pre>"},{"location":"manual/signingful/#deleting-tokens","title":"Deleting Tokens","text":"<p><code>charlie</code> can delete the token with:</p> <pre><code>curl -s -X DELETE $charlie/token/$tokenId | jq\n</code></pre>"},{"location":"manual/signingful/#docker","title":"Docker","text":"<p>Build the docker image with:</p> <pre><code>cd off_chain\ndocker build -f docker/Dockerfile -t mpf-service .\n</code></pre> <p>There is a docker-compose file that will run the service with yaci.</p> <p>You will have port 3000 and 3002 and 3004 as users. Be careful to stop yaci before running the docker compose.</p> <p>Shut down the docker compose with:</p> <pre><code>docker compose -f docker/docker-compose.yaml down --volumes\n</code></pre>"},{"location":"manual/signingless/","title":"Signingless MPFS Manual","text":""},{"location":"manual/signingless/#introduction","title":"Introduction","text":"<p>This document provides a detailed manual for the signingless version of the Merkle Patricia Forestry Service (MPFS). It is intended for users who want a service that is able to create and balance transactions and delegate the signing to the user.</p> <p>The typical interaction is getting the intended CBOR for the user goal, sign it locally with a wallet and submit it to the network. The API is public, no need to authenticate to use it and so it's the operator responsibility to protect it from DDoS attacks. In particular it exposes universally useful endpoints, like submitting any transaction.</p> <p>For the time being only preprod setup is available.</p> <p>A preprod public instance is running at https://mpfs.plutimus.com, free to use, but with no guarantee of availability. Cardano is a permissionless public network, so anyone can (should) run their own instance of the service.</p> <p>For the rest of the manual we suppose the user has control of an address which we will refer to as <code>$ADDR</code>, and that the user has a wallet that can sign transactions spending from that address.</p> <p>In the repository it's included a simple wallet functionality which we will use for the sake of getting things done.</p>"},{"location":"manual/signingless/#preprod-wallet-setup-optional","title":"Preprod Wallet Setup (optional)","text":""},{"location":"manual/signingless/#wallet-creation","title":"Wallet Creation","text":"<pre><code>git clone https://github.com/cardano-foundation/mpfs.git\ncd mpfs/off_chain\nmkdir tmp\nnpx tsx src/service/wallet.ts create-wallet tmp/test.json\n</code></pre> <p>Now you have some mnemonics in <code>tmp/test.json</code> file, you can use them to sign transactions.</p>"},{"location":"manual/signingless/#address-funding","title":"Address Funding","text":"<p>You can get some test ada to your address using the Cardano Preprod Faucet.</p> <p>Get your address with:</p> <pre><code>ADDR=$(npx tsx src/service/wallet.ts reveal-address tmp/test.json | jq -r '.address')\necho $ADDR\nexport ADDR\n</code></pre> <p>Go to Faucets, paste your address and select preprod.</p> <p>After several seconds (20 on average, up to minutes) your address will contain a UTxO with 10000 tAda. You can verify it on an explorer:</p> <pre><code>brave \"https://preprod.cardanoscan.io/address/$ADDR\"\n</code></pre>"},{"location":"manual/signingless/#manual-library","title":"Manual Library","text":""},{"location":"manual/signingless/#wait-for-a-transaction","title":"Wait for a Transaction","text":"<p>You can use the following command to wait for a transaction to be included in the blockchain:</p> <pre><code>wait_for_tx() {\n  local txId=$1\n  while true; do\n    result=$(curl -s -X 'GET' \"https://mpfs.plutimus.com/transaction?txHash=$txId\")\n    if [[ $result == *\"404 Not Found\"* ]]; then\n      echo \"Transaction $txId not found, waiting...\"\n      sleep 2\n    else\n      echo \"Transaction $txId found!\"\n      break\n    fi\n  done\n}\n</code></pre>"},{"location":"manual/signingless/#sign-and-submit-a-transaction","title":"Sign and Submit a Transaction","text":"<p>This is the most common operation. Once you receive a CBOR encoded transaction from the service, you can sign it with your wallet and submit it to the network.</p> <pre><code>sign_and_submit() {\n  local file=\"tmp/tx.cbor\"\n  local txId=$(npx tsx src/service/wallet.ts sign-transaction tmp/test.json $file)\n  result=$(curl -s -X 'POST' \\\n    \"https://mpfs.plutimus.com/transaction\" \\\n    -H 'accept: application/json' \\\n    -H 'Content-Type: application/json' \\\n    -d '{\"signedTransaction\":\"'\"$(cat $file)\"'\"}')\n  echo $result | jq -r '.txHash'\n}\n</code></pre>"},{"location":"manual/signingless/#inspect-the-current-token","title":"Inspect the Current Token","text":"<p>You can inspect the current state of a token by its ID. This will return the token's root hash, owner, and other relevant information.</p> <pre><code>inspect_token() {\n    local tokenId=$TOKEN_ID\n    result=$(curl -s -X 'GET' \\\n        \"https://mpfs.plutimus.com/token/$tokenId\" \\\n        -H 'accept: application/json')\n    echo $result | jq\n}\n</code></pre>"},{"location":"manual/signingless/#token-boot","title":"Token Boot","text":"<p>This transaction is meant to start a fresh MPF token. The requester goal is to become the owner of the token. As with all transaction endpoints the requester is supposed to pass an address where the service will select the necessary UTxOs for the fees.</p> <pre><code>result=$(curl -s -X 'GET' \\\n  \"https://mpfs.plutimus.com/transaction/$ADDR/boot-token\" \\\n  -H 'accept: application/json')\n</code></pre> <p>In the result you will find both the unsigned transaction and the token ID that will be created. You can save the unsigned transaction to a file:</p> <pre><code>echo $result | jq -r '.unsignedTransaction' &gt; tmp/tx.cbor\n</code></pre> <p>And export the token ID for the next steps:</p> <pre><code>TOKEN_ID=$(echo $result | jq -r '.value')\necho \"Token ID: $TOKEN_ID\"\n</code></pre> <p>Warning</p> <p>When using an untrusted instance you should inspect the unsigned transaction before signing it. You can do this by using the <code>cardano-cli</code> tool or any other CBOR inspection tool.</p> <p>Now you can sign and submit the transaction:</p> <pre><code>txId=$(sign_and_submit)\necho \"Transaction ID: $txId\"\n</code></pre> <p>And wait for it to be included in the blockchain:</p> <pre><code>wait_for_tx $txId\n</code></pre> <p>You can inspect the token state with:</p> <pre><code>inspect_token\n</code></pre> <p>Critically the token root is the empty hash and the owner is your public key hash. Also notice the empty set of requests, meaning no change-requests have been made yet.</p>"},{"location":"manual/signingless/#requesting-changes","title":"Requesting Changes","text":"<p>For the sake of the manual we are not going to impersonate different roles, but you can imagine that requests for changes are made by different actors, which means different wallets, addresses and public key hashes. Specifically we are reusing ADDR env var and tmp/test.json mnemonics considering the token owner as requesting changes to himself.</p>"},{"location":"manual/signingless/#request-to-insert-a-fact","title":"Request to Insert a Fact","text":"<p>A fact in this context is a key-value pair. MPFS imposes no semantics so you are free to pass a JSON string for both. In case of binary data some encoding could be necessary, MPFS is not supporting any at the moment.</p> <p>You can ask the owner to insert a fact with:</p> <pre><code>result=$(curl -s -X 'POST' \\\n  \"https://mpfs.plutimus.com/transaction/$ADDR/request-insert/$TOKEN_ID\" \\\n  -H 'accept: application/json' \\\n  -H 'Content-Type: application/json' \\\n  -d '{\n  \"key\": \"exampleKey\",\n  \"value\": \"exampleValue\"\n   }')\necho $result | jq -r '.unsignedTransaction' &gt; tmp/tx.cbor\ntxId=$(sign_and_submit)\nwait_for_tx $txId\n</code></pre> <p>Let's store the first output reference of the transaction for later use inside retracting the request:</p> <pre><code>OUTPUT_REF=\"$txId-0\"\n</code></pre> <p>Now you can inspect the token state again to see the request has been added:</p> <pre><code>inspect_token | jq -r '.requests'\n</code></pre>"},{"location":"manual/signingless/#retracting-a-request","title":"Retracting a Request","text":"<p>The owner of the request can retract it with:</p> <pre><code>result=$(curl -s -X 'GET' \\\n  \"https://mpfs.plutimus.com/transaction/$ADDR/retract-change/$OUTPUT_REF\")\necho $result | jq -r '.unsignedTransaction' &gt; tmp/tx.cbor\ntxId=$(sign_and_submit)\nwait_for_tx $txId\n</code></pre> <p>Now inspecting the token will show that the request has been removed:</p> <pre><code>inspect_token | jq -r '.requests'\n</code></pre>"},{"location":"manual/signingless/#processing-a-request","title":"Processing a Request","text":"<p>Let's re-create the request to insert a fact, but this time we will process it.</p> <pre><code>result=$(curl -s -X 'POST' \\\n  \"https://mpfs.plutimus.com/transaction/$ADDR/request-insert/$TOKEN_ID\" \\\n  -H 'accept: application/json' \\\n  -H 'Content-Type: application/json' \\\n  -d '{\n  \"key\": \"exampleKey\",\n  \"value\": \"exampleValue\"\n   }')\necho $result | jq -r '.unsignedTransaction' &gt; tmp/tx.cbor\ntxId=$(sign_and_submit)\nwait_for_tx $txId\n</code></pre> <p>Now as token owner we can process it into the token state with:</p> <pre><code>OUTPUT_REF=\"$txId-0\"\nresult=$(curl -s -X 'GET' \\\n  \"https://mpfs.plutimus.com/transaction/$ADDR/update-token/$TOKEN_ID?request=$OUTPUT_REF\" \\\n  -H 'accept: application/json')\necho $result | jq -r '.unsignedTransaction' &gt; tmp/tx.cbor\ntxId=$(sign_and_submit)\nwait_for_tx $txId\n</code></pre> <p>Now inspecting the token will show that the request has been processed as the state is not the empty hash:</p> <pre><code>inspect_token | jq -r '.state.root'\n</code></pre> <p>Moreover the MPFS service is storing the facts of each token by indexing the blockchain so we can query them with:</p> <pre><code>result=$(curl -s -X 'GET' \\\n  \"https://mpfs.plutimus.com/token/$TOKEN_ID/facts\" \\\n  -H 'accept: application/json' \\\n  -H 'Content-Type: application/json')\necho $result | jq\n</code></pre>"},{"location":"manual/signingless/#request-to-update-a-fact","title":"Request to Update a Fact","text":"<p>Anyone can request to update a fact by requesting an update to the existing key-value pair. The request is similar to the insert request, but it will update the value of an existing key.</p> <pre><code>result=$(curl -s -X 'POST' \\\n  \"https://mpfs.plutimus.com/transaction/$ADDR/request-update/$TOKEN_ID\" \\\n  -H 'accept: application/json' \\\n  -H 'Content-Type: application/json' \\\n  -d '{\n  \"key\": \"exampleKey\",\n  \"oldValue\": \"exampleValue\",\n  \"newValue\": \"newExampleValue\"\n   }')\necho $result | jq -r '.unsignedTransaction' &gt; tmp/tx.cbor\ntxId=$(sign_and_submit)\nwait_for_tx $txId\n</code></pre> <p>The token owner can process the request in the same way as before:</p> <pre><code>OUTPUT_REF=\"$txId-0\"\nresult=$(curl -s -X 'GET' \\\n  \"https://mpfs.plutimus.com/transaction/$ADDR/update-token/$TOKEN_ID?request=$OUTPUT_REF\" \\\n  -H 'accept: application/json')\necho $result | jq -r '.unsignedTransaction' &gt; tmp/tx.cbor\ntxId=$(sign_and_submit)\nwait_for_tx $txId\n</code></pre> <p>Now inspecting the facts at 'exampleKey' will show the updated value:</p> <pre><code>result=$(curl -s -X 'GET' \\\n  \"https://mpfs.plutimus.com/token/$TOKEN_ID/facts\" \\\n  -H 'accept: application/json' \\\n  -H 'Content-Type: application/json')\necho $result | jq -r '.exampleKey'\n</code></pre>"},{"location":"manual/signingless/#request-to-delete-a-fact","title":"Request to Delete a Fact","text":"<p>Anyone can request to delete a fact by requesting a deletion of the key-value pair.</p> <pre><code>result=$(curl -s -X 'POST' \\\n  \"https://mpfs.plutimus.com/transaction/$ADDR/request-delete/$TOKEN_ID\" \\\n  -H 'accept: application/json' \\\n  -H 'Content-Type: application/json' \\\n  -d '{\n  \"key\": \"exampleKey\",\n  \"value\": \"newExampleValue\"\n   }')\necho $result | jq -r '.unsignedTransaction' &gt; tmp/tx.cbor\ntxId=$(sign_and_submit)\nwait_for_tx $txId\n</code></pre> <p>The token owner can process the request in the same way as before:</p> <pre><code>OUTPUT_REF=\"$txId-0\"\nresult=$(curl -s -X 'GET' \\\n  \"https://mpfs.plutimus.com/transaction/$ADDR/update-token/$TOKEN_ID?request=$OUTPUT_REF\" \\\n  -H 'accept: application/json')\necho $result | jq -r '.unsignedTransaction' &gt; tmp/tx.cbor\ntxId=$(sign_and_submit)\nwait_for_tx $txId\n</code></pre> <p>Now facts will not contain the deleted key:</p> <pre><code>result=$(curl -s -X 'GET' \\\n  \"https://mpfs.plutimus.com/token/$TOKEN_ID/facts\" \\\n  -H 'accept: application/json' \\\n  -H 'Content-Type: application/json')\necho $result | jq\n</code></pre> <p>And the token state will be back to a null hash:</p> <pre><code>inspect_token | jq -r '.state.root'\n</code></pre>"},{"location":"manual/signingless/#deleting-a-token","title":"Deleting a Token","text":"<p>The owner of the token can delete the token making impossible to refer to its facts in other smart contracts with:</p> <pre><code>result=$(curl -s -X 'GET' \\\n  \"https://mpfs.plutimus.com/transaction/$ADDR/end-token/$TOKEN_ID\" \\\n  -H 'accept: application/json')\necho $result | jq -r '.unsignedTransaction' &gt; tmp/tx.cbor\ntxId=$(sign_and_submit tmp/tx.cbor)\nwait_for_tx $txId\n</code></pre> <p>And that's it, the token is deleted and its facts are no longer accessible.</p>"}]}